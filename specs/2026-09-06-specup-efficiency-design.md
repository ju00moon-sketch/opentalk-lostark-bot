# 스펙업 효율 `/스펙업` — 설계 (2026-09-06 · 2026-09-07 6차 개정: 크리스탈 시세 자동 갱신)

사용자 요청: "스펙업효율 구현하고 싶다." 평가 기준은 **점수 증가량**(A안, 2026-09-06 결정) — 각 후보를 "올리면 로펙 점수가 얼마나 오르는가"로
평가한다는 뜻이며(현재 상태 체크리스트가 아님), **순위 지표는 그 증가량을 기대 비용으로 나눈 `점수/10만G`**다. 증가량 자체는 각 행에 `(+증가분)`으로
함께 보여 준다. 구현 방식은 **로펙(lopec.kr) 스펙업 가이드의 계산 모듈을 우리 격리 프로세스에서 그대로 실행**하는 것(1안)이다. 성공 기준은 "로펙 스펙업 가이드 표와 같은 항목·같은 점수"이며,
**기대 비용만 공식 경매장·거래소 API의 실시간 시세**로 계산한다(사용자 요청: "경매장 API 활용해서 실시간 검색해서 골드값 효율 따져서").
따라서 점수 열은 로펙과 동일하고, 비용과 `점수/10만G`는 시세 갱신 시차만큼 로펙 표와 다를 수 있다 — 의도된 차이이며 출력에 명시한다.

조사 결과(2026-09-06): 로펙에는 `/character/specupGuide/<닉>` 탭이 있고 표 열은 **종류 · 옵션 · 최종 점수 · 기대 비용 · 점수/10만G**,
하위 탭은 스펙업 추천 / 스펙업 목표 / 강화 최적화, 토글은 페온 제외 · 작열 제외 · 보유 재료 편집이다. 결과는 서버 HTML(RSC payload)에 없고
브라우저가 계산한다. 입력은 우리가 팔찌·아크그리드 효율에 이미 쓰는 `lostarkParser`(장비·악세·어빌리티 스톤·보석·아크패시브·아크그리드·
스킬 포함)와 `POST /api/auction/prices`(시세)이며, `POST /api/specup/shared-enhancement-baseline`도 호출한다(우리 브라우저에서는 400으로 실패해
표가 "계산중"에 머물렀다 — 7절의 확인 항목). 화면 렌더링 코드(청크 6772)에는 표 헤더·토글만 있고 **계산 코드는 페이지 진입 후 동적으로 받는
청크 또는 워커**에 있다.

## 1. 목표와 비목표

목표
- `/스펙업 [닉네임]`으로 로펙 "스펙업 추천" 탭과 같은 항목·같은 점수를 보여 준다. 각 후보의 점수 증가량을 기대 비용으로 나눈 `점수/10만G` 내림차순,
  디스코드 상위 10개·카톡 미리보기 상위 5개(정렬 세부 규칙은 2절).
- 기대 비용은 공식 API 실시간 시세(5분 캐시)로 계산하고, 우리 API로 값을 못 구하는 항목만 로펙 시세로 보충한다.
- 디스코드 임베드와 카톡 평문 모두 지원(카톡 가독성 규칙: 핵심 수치 위, 항목 사이 빈 줄, 공백 정렬 표 금지, 1,500자 초과분은 전체 보기 링크).

비목표(v1)
- 로펙의 "스펙업 목표"·"강화 최적화" 탭, 보유 재료 편집.
- 페온 제외·작열 제외를 사용자가 바꾸는 옵션 — 로펙 기본값으로 고정한다(기본값은 Phase 0에서 확인).
- 서포터. 로펙이 이 페이지들에 서폿 계산 모듈을 싣지 않으므로(팔찌 효율과 같은 제약) `null` → 실패 문구.
- 자체 점수 시뮬레이션 공식. 로펙 모듈을 대체하지 않는다.

## 2. 사용자 흐름과 출력

명령
- 슬래시 `/스펙업 [닉네임]` (닉네임 생략 시 `/등록` 캐릭터 → 서버 닉네임 폴백, `resolveCharacter` 규칙 그대로)
- 초성 별칭 `ㅅㅍㅇ` (`text-commands.js` ALIASES 한 줄 → 채팅 `ㅅㅍㅇ 닉`, 슬래시 `/ㅅㅍㅇ` 자동 생성)
- 카톡 `/스펙업 닉` · `.스펙업 닉` (매처가 `commands/index.js`를 자동으로 받으므로 추가 작업 없음)
- 옵션 없음. 슬래시 커맨드 수 86 → 88 (`/스펙업`, `/ㅅㅍㅇ`) → 배포 시 재등록 필요.

정렬·개수
- 코어가 준 행은 이미 정렬돼 있다(4절): `per100k` 내림차순, **기대 비용 0G인 행(`per100k: null`)은 맨 앞**(공짜 이득이 어떤 유료 이득보다 앞), 같은 값은
  증가분 내림차순. 명령은 순서를 바꾸지 않고 플랫폼별 미리보기 개수만 적용한다.
- 증가분이 0 이하인 후보는 코어가 이미 뺐으므로 표시 대상에 없다.
- 카톡: **미리보기는 상위 5개**, 전체 보기 페이지(`/p/full/<id>`, 30분)에는 **모든 후보와 변경 후 총점**을 제공한다. 미리보기는 옵션 변경·예상 비용·점수 상승·투자 효율을 줄별로 구분한다.
  전체 평문은 별도 필드로 넘기며(3절의 브리지 훅), 후보가 5개 이하여도 변경 후 총점을 볼 수 있도록 전체 보기 링크를 제공한다. 후속 안내와 링크를 포함한 최종 메시지의 1,500자 제한은 유지한다.

디스코드 임베드(수치는 형태 예시)
```
❙ 블레상돈님의 스펙업 효율
블레이드 (1,775.00) · 현재 로펙 5,115.78

1. 악세 · 반지 2 · 치적 [하]/치피 [중] → 치적 [중]/치피 [중]  5,135.0 (+19.2) · 38,000G · 50.5*
2. 보석 · 겁화 9 → 10 (블레이드 버스트)                      5,128.1 (+12.3) · 45,000G · 27.3
3. 강화 · 완갑 8 → 9 (목표 11)                               5,131.5 (+15.7) · 71,000G · 22.1
…
마지막 열 = 점수/10만G
```
- 한 행 = `순위. 종류 · 옵션  최종점수 (+증가분) · 기대비용G · 점수/10만G`. 증가분 = `finalScore − baseScore`.
- `종류`는 로펙 표 1열 그대로(이번 표본의 실제 값: 악세·강화·보석·카르마·스톤. 각인·상재·아크그리드도 표시 함수에 존재). `옵션`은 로펙 표 2열을 기반으로
  **주대상(부위·번호·스킬명·돌 조합)과 현재 → 다음 행동, 권장 목표가 다음 행동과 다르면 `(목표 N)`**까지 포함해 같은 이름의 행이 구별되게 코어가 조합한다.
- 반올림: 점수·증가분 소수 1자리, 비용은 원값이 소수여도 **천 단위 정수로 반올림**, `점수/10만G` 소수 1자리. 반올림은 명령에서만 한다(코어는 원값).
- 기대 비용 0G 행(진짜 무료 — 4절의 무료 판정을 통과한 행만): 비용 자리에 `무료`, `점수/10만G` 자리에 `—`.
- 푸터 구성(순서 고정): `점수는 로펙 스펙업 가이드와 동일` · `비용은 거래소·경매장 실시간 시세(5분 캐시)` · `시세 기준 HH:MM`(`pricedAt`이 null이면 생략) ·
  `로펙 시세 갱신 지연`(`priceStale`일 때) · `* 로펙 시세`(로펙 시세 행이 있을 때) · `스톤·카르마는 로펙 고정 비용 모델`(`fixed` 행이 있을 때) ·
  `비용 미확인 후보 N개 제외`(`droppedForPrice > 0`). `fixed` 행에는 `*`를 붙이지 않는다.
  **페온 항목(2026-09-06 5차 개정 · 2026-09-07 6차 개정)** — `스톤·카르마는 로펙 고정 비용 모델` 뒤, `비용 미확인 후보` 앞에 하나를 넣는다: 크리스탈 시세가 있고 코어가 그 값을 썼으면
  자동 갱신값은 `페온 포함 — 크리스탈 95개 16,910G(로아툴 9/7 자동)`, 직접 설정값은 `페온 포함 — 크리스탈 95개 16,626G(9/6 설정)`(항목 안에는 ` · `를 쓰지 않는다 — 카톡이 푸터를 ` · `로 나눈다),
  값이 7일을 넘었으면 이어서 `크리스탈 시세 N일 전 값 — 자동 갱신이 안 되고 있어요, /크리스탈로 직접 갱신`, 시세가 아직 없으면(첫 자동 갱신 전·계속 실패 중)
  `페온 제외 — 크리스탈 시세를 아직 못 받았어요(/크리스탈로 직접 설정 가능)`. 카톡은 푸터를 줄 단위로 나누는 기존 규칙 그대로.
- **`/크리스탈 [가격]`(별칭 `ㅋㄹㅅㅌ`, 2026-09-06 5차 개정)**: 페온 환산에 쓰는 크리스탈 시세를 조회·설정한다. 값의 뜻은 **화폐거래소에서 골드로 블루 크리스탈
  95개를 살 때 가격**(게임 안 거래 단위이자 로펙 입력값과 같은 기준). 인자 없음 → 현재 값·설정 시각(KST)·설정자·페온 1개 환산가·갱신 방법, 7일 초과면 갱신 안내.
  인자 있음 → 정수 1,000~500,000만 받아 저장하고 확인 문구(페온 1개 환산가 포함). 권한 제한은 두지 않는다(길드 봇, 설정자·시각을 보여 주는 것으로 대신). 카톡도
  `/크리스탈 16626`으로 동일. 저장은 저장소 루트 `crystal-price.json`(gitignore, 원자 저장·0600, 배포 시 보존).
  **6차 개정(2026-09-07)**: 봇이 시작 시 1회 + 매일 08:00 KST에 로아툴(LOSPI) 시세를 자동으로 저장하므로(5-1절) 사람이 적지 않아도 페온 포함으로 계산된다. 조회 문구는 자동 갱신값
  (`9/7 08:00 자동 갱신 · 로아툴`)과 직접 설정값(`9/6 22:39 직접 설정 · 닉`)을 구분하고, 미설정 문구는 "아직 못 받았어요 + 매일 08:00 자동 갱신 안내 + 직접 설정 방법", 7일 초과는 "자동 갱신이 안 되고 있어요".
  직접 적은 값은 **24시간 동안 자동 갱신보다 우선**하고 그 뒤 자동 갱신이 덮는다(저장 파일 `source: 'manual' | 'auto'`, `by`는 설정자 닉 또는 `로아툴`; source 없는 옛 파일은 수동으로 본다).
  명령 정의(옵션)는 변경 없음 → 재등록 불필요.
- 후속 조회 버튼: 기존 `run:커맨드:닉` 규칙으로 `/로펙`·`/팔찌`·`/젬효율`(디스코드 전용 — 카톡 평문에는 2026-09-06부터 후속 안내를 넣지 않음).

카톡 평문 — 상위 5개를 비용·상승량 중심으로 표시하고 투자 효율 설명은 마지막에 한 번만 제공:
```
❙ 블레상돈님의 스펙업 효율
블레이드 (1,775.00) · 현재 로펙 5,115.78

1위 · 반지 2 교체
치적 하 → 중 · 치피 중 유지

예상 비용: 38,000골드*
점수 상승: +19.2점
투자 효율: 50.5점 / 10만 골드
…
투자 효율은 같은 골드 대비 점수 상승량입니다.
높을수록 가성비가 좋습니다.

점수는 로펙 스펙업 가이드와 동일
비용은 거래소·경매장 실시간 시세(5분 캐시)
시세 기준 14:32
* 로펙 시세
```
- 전체 보기는 같은 양식에 `변경 후 점수: 5,135.0점`을 덧붙인다. 무료 행은 `예상 비용: 무료`·`투자 효율: —`이며 효율 단위를 붙이지 않는다. 시세 보충 표시는 비용 뒤에 붙인다.
- 악세의 확인 가능한 등급 표기만 변경·유지·추가·제거로 요약한다. 알 수 없는 형식은 원문을 유지하고, 강화 등 다른 종류도 권장 목표를 생략하지 않는다. 디스코드 표기·반올림·계산·정렬은 변경하지 않는다.
실패·빈 결과 문구 (판별 순서대로)
1. 캐릭터 없음: 명령이 코어를 부르기 **전에** 공식 API `getCharacterProfile(닉)`(호출 1회)로 존재를 확인한다. 없으면 기존 `NOT_FOUND_HINT`. 코어는 이 판별을
   하지 않는다(코어의 `null`은 아래 2번으로만 해석).
2. 코어가 `null`: `` `닉네임` — 로펙에서 이 캐릭터의 스펙업 가이드를 계산할 수 없어요. 로펙 미갱신·서포터(미지원)·계산 시간 초과·시세 조회 실패 중 하나예요. 잠시 후 다시 시도해 주세요 ``
   — 4절의 `null` 사유(로펙 미갱신·서포터·진입점 실패·시간 초과·시세 전체 실패·시세 누락으로 후보 전부 제외·결과 크기 초과)를 모두 포괄한다.
3. 코어가 `rows: []`(정상 빈 결과): `` `닉네임` — 지금 추천할 스펙업이 없어요 (로펙 스펙업 가이드에 후보가 없음) ``
4. 카톡 예산 초과: 기존 브리지 경로("다시 보내 주세요" + 결과 3분 보류 캐시).

## 3. 구조와 데이터 흐름

```
/스펙업 닉 ─▶ commands/specup.js ─▶ lopec-sim.getSpecupGuide(닉, { crystalPrice95 })  (결과·진행 중 요청은 (닉, 크리스탈 가격)별 5분 캐시·공유)
                                        │
                                        ├─ lopec.getSpecPointHtml(닉) ──▶ lostarkParser (5분 캐시, 기존)
                                        ├─ ensureRuntime(html) ──▶ 모듈 묶음 + 진입점 { bangle, arkgrid, specup }  (배포 키 캐시, 기존 확장)
                                        ├─ specup-prices.collect(요구 시세 키) ──▶ 공식 API(경매장·거래소, 전역 5분 캐시) → 부족분 로펙 /api/auction/prices
                                        └─ 격리 자식 1개를 살려 두고 probe → phase A → phase B 를 stdin 메시지로 순차 처리  (격리: 권한 모델·코드 생성 금지·B안 OS 사용자, 단계별 계산 20초·전체 수명 60초)
                                                  │
                                        정규화된 SpecupGuide ◀─┘   ──▶ 명령이 정렬 유지·플랫폼별 미리보기·반올림·임베드/평문 렌더링
```

- **시세 주입(Phase 0 확정)**: 계산 진입 함수(스파이크 시점 이름 `tX(parser, options)`)의 `options`로 넣는다 —
  `enhancementMarketPriceSnapshot: { snapshotId, prices: { [materialId]: G/개 } }`, `auctionPriceSnapshot: { snapshotId, pricesByTarget: { [target]: G/1개(권) } }`,
  `excludeScorchGemUpgrades: true`(함수 기본값은 false라 **반드시 명시**), 페온은 5차 개정 이후 크리스탈 시세가 있으면 `excludePeonCost: false, peonCrystalPrice95Gold: P`,
  없으면 `excludePeonCost: true, peonCrystalPrice95Gold: null`(세 단계 동일). 재련 `prices`는 **개당** 골드다.
  추천 뷰 함수(`tJ(snapshot, ownedMaterialValues, …)`)의 두 번째 인자는 시세가 아니라 **보유 재료 수량**이므로 전부 "0"으로 둔다(보유 재료 미반영).
- **2단계 평가 — 후보는 로펙 시세로 고정, 비용만 우리 시세로**: 로펙은 효율순 정렬 뒤 `recommendationFamilyKey`별 첫 행을 표시 후보로 고르므로 시세가 표시 대상
  선택에 관여한다. "로펙 표와 같은 후보·같은 점수"를 지키기 위해 ① 로펙 공개 시세(`GET /api/auction/enhancement`, `POST /api/auction/prices`)로 한 번 계산해
  표시 후보의 **식별자 집합을 고정**하고(= 로펙 표), ② 같은 계산을 우리 시세(공식 API 우선, 부족분 로펙)로 다시 돌려 **같은 식별자의 `costValue`만** 가져온다.
  점수는 시세와 무관하므로 두 단계에서 같다. 식별자는 로펙 후보 객체의 고유 필드가 있으면 그것, 없으면 (`recommendationFamilyKey`, 주대상, 현재 → 목표)의 조합.
  ②에서 어떤 식별자를 찾지 못하면 그 후보는 `droppedForPrice`로 뺀다.
- **격리 자식 1회 기동(2026-09-06 개정, 응답 시간 단축)**: 2단계 평가는 그대로 두되 격리 자식을 단계마다 새로 띄우지 않는다. 자식 하나를 살려 두고 호스트가
  stdin으로 메시지를 순서대로 보낸다 — ① `probe`(요구 시세 키 수집) → 호스트가 로펙 시세 조회 → ② phase A(로펙 시세, 후보·점수 고정) → 호스트가 **phase A 선택
  후보의 요구 키만** 공식 시세 조회(5절, 예산 14 유지) → ③ phase B(우리 시세, 같은 식별자의 `costValue`) → 호스트가 입력을 닫고 자식이 종료한다. 번들 평가는
  한 번만 한다. 메시지는 한 줄 JSON이고 자식은 메시지마다 한 줄 JSON으로 답한다. 자식은 여전히 네트워크·환경변수·`process` 없이 돌며 호스트가 주는 시세 외의
  입력을 받지 않는다. `src/lopec-sandbox.js`는 기존처럼 입력 EOF 뒤 1회 실행·종료하는 경로(팔찌·아크그리드)를 그대로 두고 스펙업 세션 모드만 추가한다.
  데이터 흐름·시세 정책·정규화(`normalizeGuide`)·공개 계약(4절)은 바뀌지 않는다. 절감 대상은 기동·번들 평가 2회분이며 계산 자체는 줄지 않는다.
- **요청 내 원본 후보 재사용(2026-09-06 2차 개정)**: 기술 담당 진단으로 남은 시간의 대부분이 **시세와 무관한 원본 후보 생성**임이 확인됐다(어댑터가 단계마다
  baseline 안의 후보 생성과 메타데이터용 raw 후보 생성을 반복해 요청당 6회; view 호출은 0~1ms). 세션 자식 안에서 원본 후보 데이터를 **요청당 한 번만** 만들고
  세 단계의 원본 baseline 입력(후보 생성기 자리)과 메타데이터 조회에 같은 데이터를 쓴다. 비용 평가·후보 선택(family)·정규화는 원본을 그대로 호출한다.
  · **재사용 범위**: 한 요청(자식 수명) 안에서, 동일한 캐릭터 입력과 동일한 계산기 버전으로 만든 원본 후보 데이터만 공유한다. 요청 간 영구 저장(프로세스 캐시·디스크)은
    이번 범위에 포함하지 않는다.
  · **재사용 금지**: 시세를 붙잡은 baseline 전체, 비용 평가 함수, view 결과는 단계 간에 공유하지 않는다. 시세별 비용·재련 전략 계산은 단계마다 그대로 수행한다.
  · **불변 조건**: 이후 단계의 평가가 저장된 후보 객체를 변경하지 않아야 한다(인수 조건 19항). 런타임에 단계마다 직렬화 비교를 하지는 않는다(비용). 로펙 배포로 후보
    생성기 계약이 바뀌어 어댑터 경계 탐지에 실패하면 기존대로 조용히 계산하지 않고 `null`과 로그로 끝낸다.
  · 계산식·시세 예산 14·결과 캐시 5분·격리 경계·메시지 프로토콜(3단계)은 바뀌지 않는다. 1차 개정(세션)과 이 개정은 같은 브랜치에서 최종 형태로 한 번에 검증·출시한다.
- **phase B 후보 축소(2026-09-06 3차 개정)**: 기술 담당 진단으로 A/B 단계 시간의 98~99%가 시세 의존 **재련 전략 풀이**이며(점수 계산·시세 선조회 후보는 기각),
  phase B가 필요로 하는 값은 phase A가 고른 식별자의 `costValue`뿐인데도 전체 후보(약 90개)의 전략을 다시 풀고 있었다. 3차 개정에서는 **phase B의 후보 생성기에
  phase A가 고른 식별자의 원본 후보만** 넘긴다(`rawCache`의 전체 후보 배열·ID 색인은 그대로 두고, 필터로 새 배열을 만들며 원본 상대 순서를 유지한다. 선택 ID 순서로
  재구성하지 않는다). probe·phase A는 전체 후보를 그대로 받는다. B의 definitions는 B 시세로 새로 생성한다(A definitions 대체 금지 — 비용 의존 메타데이터가 달라질 수 있다).
  · **계약 결정 — phase B 내부 배열 순서는 공개 계약이 아니다.** 원본 추천 함수가 필터 뒤 순번으로 필터 전 정의 배열의 `requestSortIndex`를 읽는 동작 때문에 동점 행의
    내부 순서가 전체 후보 계산과 달라질 수 있다. 정규화(`normalizeGuide`)는 B를 ID 색인으로 읽고 A의 후보 순서를 따르므로 이 차이는 최종 결과에 전달되지 않는다. 따라서
    B의 동등성은 **비배열 필드(`baseScore`)·후보 ID 집합·ID별 전체 필드**로 판정하고, probe·A 반환과 최종 공개 반환·최종 추천 순서·양 플랫폼 출력은 계속 엄격히 같아야 한다.
  · **전제와 탐지 정책**: 이 개정은 "후보 하나의 비용이 다른 후보와 무관하다"는 원본 동작에 기댄다(3명 고정 입력 로컬·운영 18쌍에서 ID별 전체 값 일치로 확인). 기존 경계
    검사는 기본값·배선만 보므로 원본 평가 본문에 후보 간 누적이 새로 생기는 의미 변경은 잡지 못한다. 그래서 **번들 검증 단계**를 둔다: 모듈 묶음(배포 키)이 새로 만들어진
    뒤 첫 성공 요청의 phase B에서 자식이 축소 B와 전체 B를 모두 계산해 선택 ID별 `costValue`·`requiredKeys`·`hasCostModel`·`fixedOnly`를 비교하고, 축소 결과와 함께
    일치 여부를 돌려준다. 일치하면 그 번들은 검증됨으로 표시해 이후 요청은 축소 B만 계산한다. 불일치하면 로그를 남기고 **그 번들에서는 전체 B로 동작**한다(축소 결과를
    조용히 쓰지 않는다). 검증 비용은 로펙 배포·프로세스 재시작마다 요청 1회에 전체 B 1회분이다. 기술 담당이 같은 보장을 주는 더 싼 방식을 찾으면 대체할 수 있다.
  · 누락 시세·빈 ID·없는 ID 처리와 `droppedForPrice` 규칙, 시세 예산 14·결과 캐시 5분·격리 경계·3단계 프로토콜은 바뀌지 않는다. 1·2·3차는 같은 브랜치에서 최종 형태로
    한 번에 검증·출시한다.
- **일반 재련 준비 데이터 재사용(2026-09-06 4차 개정 — 구현·검증 뒤 미채택, 기록용)**: 기술 담당 진단으로 phase A의 남은 시간 대부분이 재련 전략 풀이 안에서
  **시세와 무관한 준비 작업**을 반복하는 데 쓰임이 확인됐다(실패 횟수별 확률 프로필 생성 7,000~7,700회 중 고유 275~330개, 보조재 선택 옵션 7,400~8,200회 중 고유
  922~996개). 4차 개정에서는 격리 자식 안에서 **한 요청 동안** 일반 재련의 확률 프로필과 전체/선택 보조재 옵션 결과만 기억해 재사용한다. 시세가 붙은 기대 비용·최적
  정책·baseline·view는 단계마다 그대로 다시 계산한다. 원본 준비 함수의 결과를 저장하는 래퍼일 뿐 확률 공식·재료 수량·경로 계산식을 새로 쓰지 않는다.
  · **범위**: 동일 요청·동일 파싱 입력·동일 규칙 번들 안에서만. parser가 바뀌면 비우고, 다른 자식·다른 요청·다른 번들과 공유하지 않는다. 결과 캐시 5분·디스크 저장 없음은 그대로.
  · **키**: 프로필은 의미 있는 입력 전체(tier·equipmentType·currentLevel·failedAttempts·artisanEnergy 등 원본 인자 전부)를 작은 키로, 전체 옵션은 프로필 객체별, 선택 옵션은
    프로필 객체별 안의 선택자 전체 키로 둔다. 캐릭터 ID·후보 ID만으로 키를 만들지 않고, 큰 프로필을 조회마다 직렬화하지 않는다. 지원하지 않는 입력(undefined·null·비유한
    수·-0 등 직렬화 충돌 가능성)은 캐시를 우회해 원본을 그대로 호출한다.
  · **변이 금지**: 공유하는 프로필·옵션은 읽기 전용이어야 하며, 준비 결과나 입력을 바꾸는 계산기 번들에서는 활성화하지 않는다.
  · **활성화 조건과 실패 시 동작**: 준비 함수의 export·호출 배선·시그니처 검사를 통과한 번들에서만 시도하고, 3차의 번들 검증과 같은 방식으로 **처음 보는 번들의 첫 성공
    요청에서 캐시를 끈 phase A와 켠 phase A를 같은 자식에서 계산해 반환 전체가 같을 때만** 그 번들에서 켠다. 불일치·검사 실패·알 수 없는 변경은 **최적화만 끄고 3차 계산으로
    돌아가며** 로그를 남긴다(기능 실패가 아니다). 첫 요청이 카톡 25초를 넘기지 않도록 B 검증(3차)과 A 검증(4차)을 첫 두 요청에 나눠 실행해도 된다 — 기술 설계에서 확정.
  · **금지**: 시세가 붙은 기대 비용·재련 정책·baseline·view 결과를 저장해 다른 시세에 재사용하지 않는다. **상급 재련 경로는 범위 밖**이며 원본 그대로 실행한다(고정 표본
    3명에서는 호출되지 않았음). 원본 계산식은 유지한다.
  · **결과(같은 날)**: 위 경계를 모두 갖춘 정식 구현(입력 키 검사·읽기 전용 복제·변이 감지·별도 VM 컨텍스트 검증·R3 복귀)은 로컬 132건·4명 동등성·원본 대조 68,216회를
    통과했지만, 운영 고정 입력 각 3회에서 R3 대비 블레상돈 −4.6% · 건슬상돈 +0.3% · 초코상돈 +0.6%로 **일관된 단축이 없었다**. 진단 시점의 약 24%는 보호 장치가 없는
    임시 래퍼의 수치였고 정식 구현에서 재현되지 않았다. 반면 두 번째 검증 요청 약 21.5초·자식 RSS 최대 약 128MiB(검증 후 반복, 첫 검증 포함 관측 최대 135.26MiB; R3 약
    106MiB)·코드와 검증 부담이 늘었다. 따라서
    **4차는 채택하지 않고 출시 후보는 3차 최종 형태(R1+R2+R3)로 한다.** 이 불릿은 시도와 근거를 남기기 위한 기록이며 제품에는 반영하지 않는다.
- **카톡 미리보기/전체 보기 분리 훅**(기획 담당 수정, 기술 담당 검토): 명령은 카톡 분기(`interaction.platform === 'kakao'`)에서만 payload에 선택 필드
  `kakaoFull`(전체 행 평문)을 싣는다. `render.toKakaoResponse`가 이 필드를 응답에 그대로 실어 주고, `handler.flattenParts`가 `{ body, tail, full }`로 꺼내며,
  `render.fitBridgeMessage(body, tail, { baseUrl, fullTitle, fullText })`는 `fullText`가 있으면 **전체 보기에는 `fullText`를 저장**하고 본문은 기존대로 1,500자에
  맞춘다. 링크 줄은 후보가 10개를 넘어(`fullText !== body`) 본문이 한도 안이어도 붙이고, 후보가 10개 이하라도 최종 메시지가 1,500자를 넘으면 기존 잘라내기 경로로 붙인다 — 2절의 조건과 같다. 필드가 없는 기존 명령은 동작이 바뀌지 않는다(회귀 기준 9절 11항).
  디스코드 분기는 이 필드를 만들지 않으므로 discord.js payload에 영향이 없다.
- **페온 포함(2026-09-06 5차 개정)**: 명령이 크리스탈 시세를 넘기면 코어는 probe·phase A·phase B 모두에 `excludePeonCost: false, peonCrystalPrice95Gold: P`를 넣는다
  (로펙 UI에 같은 값을 입력한 "페온 포함" 보기와 같은 후보·비용 — 로펙 모델은 악세 교체 35페온, 스톤 1개당 9페온, 페온 1개 = P ÷ 95 × 8.5골드, 보석·강화·카르마 무영향).
  넘기지 않으면 기존대로 페온 제외. 결과 캐시 키에 P를 포함해 시세를 바꾸면 다음 조회부터 반영된다. 값은 저장소 `crystal-price.json`에서 온다 — 5차는 `/크리스탈` 수동 설정만,
  6차(2026-09-07)부터 로아툴 자동 갱신 + 24시간 수동 우선(5-1절). 코어는 값의 출처를 구분하지 않는다.
- **진입점 탐색**: `lopec-sim.js`의 기존 `findEntries`에 `specup`을 추가한다. 초기 `<script>`만으로는 부족하고 웹팩 런타임의 동적 청크 맵에서 추천 코어 청크를
  추가 수집한다. 별도 워커 청크(강화 워커·OCR)는 같은 모듈 ID를 다른 export로 배포하므로 **런타임별 모듈 그래프를 섞지 않는다**(섞으면 초기화 실패). 탐색은 소스
  패턴(`enhancementMarketPriceSnapshot:`을 옵션으로 받는 함수)으로 하고, 본문에 `auctionPriceSnapshot`·`initialRecommendationView`가 있는지 교차 검증한다. 후보가
  0개거나 여러 개면 `null`로 끝낸다(조용히 계산하지 않는다). 계산 자체는 동기 함수라 워커가 필요 없다(Phase 0 확인).

## 4. 인터페이스 계약 — 명령과 코어의 경계

```js
// src/lopec-sim.js (기술 담당)
export function getSpecupGuide(characterName, { crystalPrice95 = null } = {}): Promise<SpecupGuide | null>
// crystalPrice95: 블루 크리스탈 95개의 골드 가격(양의 유한수)이면 페온 포함, 그 외(null·0·음수·비수)는 페온 제외로 계산한다(5차 개정).

SpecupGuide = {
  baseScore: number,        // 현재 로펙 점수(반올림 전). 로펙 페이지 표시 점수와 오차 ≤ 0.01
  pricedAt: number|null,    // 이번 비용에 실제로 쓴 **시장 시세**의 기준 시각(epoch ms) 중 가장 오래된 것 — api는 수집 시각, lopec은 snapshotId의 원본 시각
                            // (`multi:` 형식은 포함된 시각 중 최소). 모든 행이 고정 비용(fixed)뿐이면 null
  priceStale: boolean,      // 로펙 시세 응답이 isStale(갱신 지연)인 값을 하나라도 썼으면 true
  droppedForPrice: number,  // 표시 후보 중 비용을 정할 수 없어 rows에서 뺀 개수 — 필요한 시세 키 누락, 비용 모델 없음(예: 아크그리드 젬), 2단계에서 식별자 미발견
  crystalPrice95: number|null, // 이번 계산에 실제로 쓴 크리스탈 95개 가격. 페온 제외로 계산했으면 null (5차 개정 — 푸터가 이 값으로 포함/제외를 표기)
  rows: Array<{             // 정렬 완료 · 작열 제외 적용, 페온은 crystalPrice95가 있으면 포함(로펙 페온 포함 보기)·없으면 제외 · 로펙 표와 같은 후보 집합 · 증가분 > 0 인 행 전체
    kind: string,           // 로펙 표 1열 "종류" 그대로 (실측 값: 악세·강화·보석·카르마·스톤)
    option: string,         // 주대상(부위·번호·스킬명·돌 조합) + 현재 → 다음 행동 + 권장 목표가 다르면 "(목표 N)" — 같은 이름의 행이 구별되게
    finalScore: number,     // 최종 점수, 반올림 전 (= baseSpecPoint + scoreGain)
    expectedCost: number,   // 로펙 후보의 costValue(골드). 0 이상 유한수, 소수 허용. 표시 문자열 cost(스톤은 "개수")·raw costGold을 쓰지 않는다
    per100k: number|null,   // (finalScore − baseScore) / expectedCost × 100000, 반올림 전. expectedCost가 0이면 null (Phase 0에서 로펙 efficiencyValue와 동일 확인)
    priceSource: 'api' | 'lopec' | 'fixed',  // 'fixed' = 시장 시세를 전혀 쓰지 않은 로펙 고정 비용 모델(스톤 50G/개는 페온 제외 기본 단가 — 페온 포함이면 개당 9페온 골드가 더해짐, 카르마 900G/회).
                            // 시장 시세를 쓴 행은 필요한 키 중 하나라도 로펙 값이면 'lopec', 전부 공식 API면 'api'
  }>,
}
```
규약
- **정렬은 코어가 끝낸다**: `per100k` 내림차순, `per100k === null`(0G) 행은 맨 앞, 같은 값은 증가분(`finalScore − baseScore`) 내림차순. 증가분이 0 이하인
  후보는 넣지 않는다.
- **수치 경계**: `expectedCost`·`finalScore`·`baseScore`는 유한수만. 비용이 `NaN`·음수·무한대로 계산된 후보는 "시세 없음"과 같이 취급해 뺀다(`droppedForPrice`에
  포함). 결과 JSON에 `Infinity`·`NaN`이 들어가지 않는다(JSON에서는 `null`로 변해 의미가 사라지므로 코어가 직렬화 전에 걸러야 한다).
- **무료와 누락의 구분**: 로펙 원본은 시세가 빠진 후보도 `cost="0"`으로 표시하므로 표시 문자열로 무료를 판정하지 않는다. 한 후보가 **무료**인 조건은
  `costValue === 0` **이고** 그 후보가 요구하는 시세 키가 모두 사용한 스냅샷에 있을 때뿐이다. 요구 키가 하나라도 없거나 비용 모델이 없는 후보(아크그리드 젬처럼
  시세 evaluator가 연결되지 않은 그룹)는 무료가 아니라 `droppedForPrice`다.
- **실패는 전부 `null`**: 로펙 미갱신·서포터·진입점 탐색 실패·격리 시간 초과·시세 전체 실패·**후보는 있었으나 시세 누락으로 전부 제외**·결과 크기 초과.
  예외를 명령으로 전파하지 않는다. 로그는 코어가 `console.error('[스펙업] …')`로 남기고, 마지막 경우는 `시세 누락으로 후보 N개 전부 제외`를 적는다.
- **정상 빈 결과와 실패의 구분**: 로펙 모듈이 낸 후보가 0개이면 `{ rows: [], droppedForPrice: 0, … }`(정상 — 명령은 "추천할 스펙업이 없어요"). 후보가 1개 이상인데
  시세 누락으로 `rows`가 비면 `null`(실패). 일부만 빠졌으면 남은 행과 `droppedForPrice > 0`을 준다.
- 반올림·천 단위 표기·상위 N 자르기·카톡 전체 평문 생성은 명령의 책임. 코어는 원값과 전체 행을 준다.
- 계약은 Phase 0 결과로 **한 번** 갱신될 수 있다(예: `per100k` 정의, `kind` 값 목록). 갱신은 이 문서 4절과 인수인계 문서에 함께 적는다.

## 5. 시세 수집 규칙 — `src/specup-prices.js` (기술 담당)

- 입력: 2단계 평가의 ①에서 고정한 표시 후보가 요구하는 시세 키 — 재련 `materialId`(27종 중 필요한 것)와 거래 `target`(`gem.{종류}.{레벨}`, `market.engraving.{각인명}`,
  `accessory.{등급조합}.{옵션키}`). 출력: 계산 모듈에 넣을 두 스냅샷과 메타 — `{ enhancement: { snapshotId, prices }, auction: { snapshotId, pricesByTarget },
  sourceByKey: { [키]: 'api'|'lopec' }, marketAt: ms|null, stale: boolean, apiCalls: number, missing: string[] }`.
- **로펙 시세를 먼저 받는다**(두 경로 모두, 전역 5분 캐시): 강화 재료는 `GET https://lopec.kr/api/auction/enhancement`, 거래 물품은 `POST …/api/auction/prices`
  본문 `{ targets: [...] }`. 이 값이 ①(후보 고정)의 입력이고 ②의 보충값이다. 응답의 `isStale`·`staleReason`은 `snapshot` 바깥 필드라 따로 보존해 `stale`로 올린다.
- **공식 API로 덮어쓴다** — 정확히 매핑되는 키만, 우선순위대로, 예산 안에서:
  1. 보석 `gem.{종류}.{레벨}` → `searchAuctionItems(210000, "N레벨 종류의 보석")`, 양수 `AuctionInfo.BuyPrice`의 최저값(0·null 제외). 작열은 수집 전에 제외.
  2. 각인서 `market.engraving.{각인명}` → `searchMarketItems(40000, 각인명, { grade })`(후보 metadata의 등급), 정확 품명·등급의 권당 현재가.
  3. 재련 재료 → 카테고리 페이지 조회(`50010` 기본 재료, `50020` 보조 재료 — 1페이지가 카테고리 전체가 아니므로 필요한 품목이 다 나올 때까지 페이지 추가),
     단가 = `CurrentMinPrice / BundleCount`, `fragment`는 **운명의 파편 주머니(대)** 기준 `÷ BundleCount ÷ 3000`(효율표의 소주머니 ÷1000 단가와 혼용하지 않는다).
  4. **악세 `accessory.*`는 v1에서 공식 API로 덮지 않는다**(로펙 target에 품질·티어·등급·부위·옵션 배치 정보가 없어 같은 최저가 조건을 재현할 수 없음) — 로펙 값 유지.
  5. 스톤·카르마는 시세 키가 없다(로펙 고정 비용 모델). 페온·크리스탈은 기본 옵션(페온 제외)에서 불필요.
- **예산**: 시세용 공식 API 호출 **≤ 14회** + 명령의 캐릭터 존재 확인 1회 = 캐릭터당 ≤ 15회. 예산에 걸리면 남은 키는 로펙 값을 그대로 쓴다.
- **보충(로펙 값 유지) 사유** — 어느 경우든 `sourceByKey`에 `lopec`으로 남긴다: 공식 API 실패·빈 응답·비양수값 · 매핑 불명(악세 등) · 예산 한도 초과.
  로펙에도 없는 키는 `missing`이고, 그 키를 요구하는 후보는 ②에서 `droppedForPrice`가 된다.
- 캐시: 전역 5분(보석 시세판·효율표와 같은 수명). 캐시가 비었을 때 **캐릭터당 공식 API 호출 ≤ 15회**, 5분 내 재호출 0회. 분당 100회 한도는 운영 봇과
  공유하므로 이 상한을 넘기는 매핑은 병합 요청 전에 줄인다.
- **진행 중 요청 공유**: 조회 묶음 단위(경매장 검색 조건 하나, 거래소 카테고리 조회 하나)로 진행 중 Promise를 보관해, 서로 다른 캐릭터의 동시 요청이 같은
  묶음을 두 번 부르지 않게 한다(보석 시세판이 쓰는 방식). 성공은 5분 캐시에 넣고, **실패는 캐시하지 않고 진행 중 항목만 지워** 다음 요청이 곧바로 재시도한다
  (실패 묶음이 결과에 필요하면 그 회차는 로펙 보충 → 그래도 없으면 해당 후보 제외).
- **시각**: 공식 API 키는 수집 시각, 로펙 키는 `snapshotId`의 원본 시각(`multi:` 형식은 포함된 ISO 시각의 최소, 파싱 실패 시 수집 시각으로 대체하고 `stale`을 true로).
  `marketAt`은 실제로 쓰인 시장 키들의 최소, 시장 키를 하나도 안 썼으면 null. 로그에는 수집 시각·원본 시각·stale을 따로 남긴다.

## 5-1. 크리스탈 시세 자동 갱신 — `src/crystal-auto.js` (기술 담당, 2026-09-07 6차 개정)

- **출처**: 로아툴(LOSPI) `GET https://loatool.taeu.kr/api/crystal-history/ohlc/1d` — 인증·쿠키 없음, 응답 `[{ dt: 'YYYY-MM-DDTHH:MM:SS'(KST 벽시계, 시간대 표기 없음), open, high, low, close }]`
  오름차순, 마지막 원소가 오늘 진행 중인 캔들이며 그 `close`가 현재 시세(블루 크리스탈 95개의 골드 가격). 브라우저형 UA로 1회 요청 시 200·약 5.6KB(2026-09-07 확인). 같은 경로의 `/1h`·`/1`은 쓰지 않는다.
- **`fetchCrystalPrice95()`** → `Promise<{ price95: number, candleAt: number } | null>`. 5초 시간 제한(연결+본문, AbortController), UA는 봇 이름(`Pogeunhaeyong/1.2 (+홈페이지 주소)`).
  검증: 배열이고 비어 있지 않음 → 마지막 원소의 `close`가 유한수 → `price95 = Math.round(close)`가 `isValidPrice`(1,000~500,000) → `candleAt = Date.parse(dt + '+09:00')`가 유효하고 지금으로부터 3일 이내.
  하나라도 어긋나면 `null`(이유 로그 1줄). 예외를 밖으로 내지 않는다.
- **`startCrystalAutoRefresh({ store = crystalStore, fetchPrice = fetchCrystalPrice95, setTimer = setTimeout, now = Date.now } = {})`** → `{ stop() }`.
  시작 즉시 1회 실행, 이후 매일 `REFRESH_HOUR_KST`(08:00 KST) 정각(모험섬 알림의 `msUntilNextKst` 방식). 성공이면 `store.setAuto(price95, now())` — 24시간 안에 직접 적은 값이 있으면 저장소가
  `{ saved: false, reason: 'manual-recent' }`를 돌려주므로 그대로 두고 로그. 실패(`null`)면 마지막 값 유지, `RETRY_MS`(1시간) 뒤 재시도(다음 정규 시각이 더 이르면 정규 시각에).
  타이머는 하나만 유지해 재시도와 정규 실행이 겹치지 않게 하고 `stop()`이 지운다. 로그는 실행마다 1줄(성공값 · 건너뜀 이유 · 실패 이유), 비밀값 없음.
- **저장 규칙(기획, `src/crystal-price.js`)**: `set(price95, by)` = 수동(`source: 'manual'`), `setAuto(price95, now, label = '로아툴')` = 자동(`source: 'auto'`, `by: label`), 수동값이 `MANUAL_HOLD_MS`(24시간) 안이면
  자동은 덮지 않는다. `get()`은 `source`를 항상 돌려준다(옛 파일 = manual). 범위 검사는 수동·자동 동일.
- **진입점(기획, `src/index.js`)**: `ClientReady`에서 `startCrystalAutoRefresh()` 호출(모험섬·업데이트 알림 뒤). 자리표시 상태에서는 아무 일도 하지 않으므로 봇은 그대로 기동한다.
- **운영 규칙**: 요청은 시작 1회 + 하루 1회(실패 재시도를 합쳐도 하루 24회 이하), 새 환경변수·의존성·폰 스크립트 변경 없음. 수요일 06:00 자동 재부팅 뒤에는 시작 시 실행이 그날 값을 채운다.
  로아툴이 응답 형식을 바꾸거나 멈추면 값이 늙어 가며 7일 뒤 안내가 붙을 뿐, 다른 명령에는 영향이 없다.
- 고정 자료: `%TEMP%/opentalk-crystal-fixtures/ohlc-1d.json`(2026-09-07 01:31 KST 실응답 63일치, 마지막 캔들 `2026-09-07T00:00:00` close 16910).

## 6. 예산·캐시·동시성·안전·관측

- 시간 예산: 디스코드는 `deferReply` 뒤 여유가 충분하다. 카톡 브리지는 총 25초. **운영 실측(2026-09-06, `b317910`, 제한 사용자 래퍼)**: 격리 자식을 단계마다 새로
  띄우는 구조에서 예열 후 새 캐릭터가 probe 3.1~3.5초 · phase A 8.7~8.9초 · phase B 8.3~9.2초, 합 20.9초(브리지 22.1초)였고, 첫 조회는 청크 수집을 더해 30.4초였다
  (로컬 3.3~3.9초의 약 6배). 기동 1회 구조(3절)의 목표는 **예열 상태 새 캐릭터 15초 이내**이며 절감 폭은 미측정이므로 실측으로 확인한다(9절 17항). 상한:
  **단계별 계산 20초**(계산 구간만, 호스트가 시세를 가져오는 동안의 대기는 제외) · **자식 전체 수명 60초**(자식 생성부터 호스트 시세 대기까지 포함, 실행 전 대기열
  시간은 별도) · 기존 팔찌·아크그리드는 15초 유지. 초과 시 기존 브리지 경로가 "다시 보내 주세요"와 3분 보류 캐시로 처리한다.
- 2차 개정(후보 재사용) 근거 수치: 1차 개정(세션)의 운영 고정 입력 A/B는 21.63→18.60초·20.53→17.57초(약 14%), 실제 예열 후 새 조회 23.80~24.63초로 15초 목표 미달.
  후보 재사용 진단본은 로컬 고정 입력에서 2,726→1,788ms·2,487→1,698ms·2,864→1,975ms(31~34%)이며 세 단계·최종 반환 deepEqual. 이 비율은 가능성 확인용이고 운영
  단축률이나 15초 달성을 보장하는 값으로 쓰지 않는다. 운영 실측(9절 20항)에서 15초 목표와 함께 **카톡 25초 예산 안에 재요청 없이 응답했는지**를 기록한다.
- 3차 개정(phase B 후보 축소) 근거 수치(기술 담당 진단, 운영 고정 입력 각 3회 중앙값, API·카톡 전달 제외): 블레상돈 전체 13.108→8.293초(B 5.206→0.424초) ·
  건슬상돈 12.010→7.724초(B 4.490→0.230초) · 초코상돈 14.071→8.731초(B 5.377→0.285초). 같은 R2가 앞선 측정에서 16.2/15.3초였듯 운영 시간은 ±25% 흔들린다.
  로컬·운영 18쌍에서 probe·A 반환, ID별 B 전체 행, 최종 공개 반환, 양 플랫폼 출력 문자열 일치. 15초 목표는 이 개정으로 고정 입력에서 달성 범위에 들어오며 실조회는
  9절 23항에서 확인한다. 기각·보류: 점수 계산 메모(A/B 계산기 호출 합 71~81ms) · 시세 선조회(후보 의존 POST가 병목, 재료판 선조회는 예산 14와 양립 불가) ·
  재련 전략 내부 준비 작업 분해(3차 뒤에도 부족할 때만) · 카톡 `/스펙업` 전용 예산 상향(3차 실측 뒤 필요 시).
- 4차 개정(재련 준비 데이터 재사용) 근거 수치(기술 담당 진단, 운영 고정 입력 각 3회 중앙값, 외부 통신 제외): R3 대비 블레상돈 10.398→7.919초 · 건슬상돈 10.121→7.677초 ·
  초코상돈 10.931→8.275초(약 24%, A 단계 6.8~7.9→4.6~5.0초). 세 단계·최종 반환·양 플랫폼 payload 동일, 캐시 히트마다 원본 재실행 대조 42,552회·규칙 125개×실패 횟수·장인의
  기운 조합·시세 교체/0/누락 9쌍 동일, 비변이 확인. 관측 최대 RSS 약 105~124MiB(상한 256MB). 첫 번들 검증 경로의 운영 시간과 상급 재련 후보가 있는 입력은 미측정 —
  9절 24~26항에서 확인한다. **정식 구현 결과(미채택)**: 운영 고정 입력 R3→R4 중앙값 11.20→10.69 / 10.52→10.55 / 11.51→11.58초, 검증 요청 18.5초(B)·21.5초(A),
  RSS 최대 128MiB(검증 후 반복)·첫 검증 포함 관측 최대 135.26MiB. 출시 후보는 3차 최종 형태.
- 로펙 요청 수: 캐릭터 페이지 1 + 시세 2(전역 5분 캐시) — 기존 `/로펙`·`/팔찌`와 같은 수준.
- 캐시(모두 메모리, 재시작 시 만료): 결과 캐릭터당 5분(2026-09-06 검토에서 30분 연장안은 채택하지 않음) · 시세 전역 5분 · 로펙 페이지와 모듈 묶음은 기존 5분·배포 키.
- 동시성: 같은 캐릭터 진행 중 요청은 하나의 Promise를 공유한다(보석 시세판과 같은 방식). 격리 프로세스는 **전역 동시 2개 상한**을 유지하고, 스펙업은 그 안에서
  **전용 동시 1개**로 제한한다(2026-09-06 개정). 확보 순서는 스펙업 전용 슬롯 → 전역 슬롯이며, 팔찌·아크그리드는 스펙업 대기열에 차단되지 않고 남은 전역 슬롯을
  쓴다. 시간 초과·수명 초과·출력 초과·프로토콜 오류 등 **모든 실패 경로에서 실제 자식 종료를 확인한 뒤** 슬롯을 돌려준다(래퍼에는 SIGTERM 중계, 래퍼 없는
  경로는 SIGKILL). 대기로 인한 카톡 예산 초과는 위 브리지 경로가 흡수한다.
- 안전: 로펙 코드 실행 격리는 기존 그대로(`--permission`, 파일은 harden만 읽기, 코드 생성 금지, 256MB, `LOPEC_SANDBOX_WRAPPER` B안). 시세는 JSON 직렬화로만
  주입하고 결과 JSON 1MB 상한을 유지한다. 새 환경변수·포트·권한 없음.
- 관측: 계산마다 한 줄 `[스펙업] 닉 · 행 N개 · 격리 X.Xs · 시세 api N/lopec M`. 로펙 보충 비율이 커지면 API 매핑 누락 신호로 본다.

## 7. Phase 0 — 기술 담당 스파이크 (결과 보고가 산출물, 제품 코드 변경 없음)

확인 항목
1. 계산 진입점: 어느 동적 청크/워커의 어떤 함수인지, `lostarkParser`와 시세를 받아 **동기 호출**로 결과를 돌려주는지, 기존 `findEntries` 방식(소스 패턴
   역추적)으로 배포마다 다시 찾을 수 있는지.
2. 입력 계약: 시세 객체의 정확한 형태(키 목록·단위·통화), `options`(페온 제외·작열 제외)의 필드명과 기본값, `shared-enhancement-baseline`의 역할과
   400의 원인 — 필수 입력인지, 없을 때 결과가 어떻게 달라지는지, 우리가 같은 값을 재현할 수 있는지.
3. 실측: 블레상돈 1회 격리 실행의 시간·최대 메모리·행 수. 점수 열(`finalScore`)과 `baseScore`가 같은 시점의 로펙 표·페이지와 일치하는지.
4. `점수/10만G` 정의 확인(4절의 기대식과 다르면 계약 갱신).
5. 시세 매핑 표: 계산이 요구하는 시세 키 ↔ 공식 API 대응(거래소 카테고리 코드·경매장 검색 조건), 대응 불가 항목 목록(로펙 보충 대상), 캐시 미스 시 호출 수 추정.

**결과(2026-09-06, 기술 담당)**: **Go.** 진입점은 추천 코어 청크의 동기 함수(워커 아님), baseline API 없이 계산 가능(무기 23→24 비용이 baseline 값과 동일),
블레상돈 로컬 실측 1.0~1.13초·최대 RSS 78.5MiB, 화면 후보 50/50 일치·기준 점수 오차 0.004·최종 점수 최대 오차 0.005. 시세 입력 형태·고정 비용·후보 묶음·
누락/무료 구분·시각 처리에 관한 7개 제안은 이 문서 2~5절과 9절에 반영했다(10절 결정 기록). 운영 서버 실측과 공식 API 예산 실측은 9절로 넘긴다.
보고서: `%TEMP%/opentalk-specup-spike-report.md`, `%TEMP%/opentalk-specup-price-analysis-20260906.md`.

Go / No-go
- Go 조건 넷 모두 충족: ① 격리 컨텍스트에서 동기 호출 가능 ② baseline 없이 결과가 나오거나 우리가 재현 가능 ③ 1회 계산 ≤ 20초·256MB
  ④ **같은 조건(같은 캐릭터·로펙 기본 토글·같은 시점)에서 후보 집합(`kind`·`option`)이 로펙 표와 같고, `baseScore`와 각 행의 `finalScore` 오차가 9절 1항과
  같은 ≤ 0.01** — 시세만 다른 상태에서 점수 열이 일치해야 1안의 전제("로펙과 같은 숫자")가 성립한다.
- 하나라도 어긋나면 구현을 시작하지 않고 사용자께 보고한다. 대안은 2안(입력을 바꿔 로펙 점수 함수로 재계산 — 후보 열거·비용 모델을 우리가 만듦)과
  3안(요청마다 헤드리스 브라우저로 로펙 표를 긁음 — 1 vCPU 무료 서버 부담으로 비권장). 선택은 사용자 결정.

## 8. 단계·브랜치·역할 (2026-09-05 역할 개정 · 2026-09-06 분리 기준 적용)

- 통합 책임자: 기획·설계·기능 담당(명령 진입점 쪽).
- 기술·검증·출시 담당 — 브랜치 `feat/specup-core`: Phase 0 스파이크 → `src/lopec-sim.js`(진입점 탐색·`getSpecupGuide`·동시 2개 상한·20초 예산),
  신규 `src/specup-prices.js`, 필요 시 `src/lopec.js` 보조 함수. 인수 테스트는 9절 1~7항.
- 기획·설계·기능 — 브랜치 `feat/specup`(워크트리 `D:\OpenTalk-specup`, 이 문서가 첫 커밋): 신규 `src/commands/specup.js`, `src/commands/index.js` 등록,
  `src/text-commands.js`의 `ㅅㅍㅇ`, `src/commands/help.js`, README·`docs/index.html` 커맨드 표 초안, 그리고 카톡 분리 훅(3절)을 위한 `src/kakao/render.js`
  (`toKakaoResponse`의 `kakaoFull` 전달, `fitBridgeMessage`의 `fullText` 옵션)·`src/kakao/handler.js`(`flattenParts`의 `full` 반환) 소규모 수정.
  이 두 파일은 기술 담당이 같은 기간에 만지지 않는다(만져야 하면 먼저 인계). **Phase 1과 병행** — 4절 계약대로 고정 행을 돌려주는
  스텁 `getSpecupGuide`로 먼저 만들고 코어가 오면 스텁만 뺀다.
- 파일 경계: 위 목록의 파일을 양쪽이 겹쳐 만지지 않는다. 계약 변경이 필요하면 코드보다 먼저 이 문서 4절을 고치고 상대에게 인계한다.
- 통합: 통합 책임자가 코어 브랜치를 자기 워크트리에 합쳐 9절의 통합 검증을 한 뒤 인계 블록으로 넘긴다. dev 머지·main 머지(=배포)·재등록·패치노트는 출시 담당.
- 교차 검토: 코어는 기획 담당이 요구사항·회귀 관점으로, 명령·출력은 기술 담당이 검토한다. 받지 않은 검토를 완료로 적지 않는다.
- 응답 시간 단축(2026-09-06 개정): 브랜치 `feat/specup-latency`(워크트리 `D:\OpenTalk-specup-latency`, dev `b317910` 기준). 기획 담당은 이 문서 개정만 커밋한 뒤
  워크트리째 인계하고 이후 손대지 않는다. 구현·통합 책임자는 기술 담당 — `src/lopec-sim.js`·`src/lopec-sandbox.js`만 수정하고 명령·표시·브리지·문서 행은
  수정하지 않는다. 인수 조건은 9절 13~17항. 카톡 가독성 개선 작업(기술 담당 브랜치, 이 문서 2절·9절 수정)과는 먼저 머지되는 쪽 위에 다른 쪽을 올린다.
- 2차 개정(후보 재사용, 2026-09-06): 같은 브랜치·워크트리에서 진행한다. 1차 개정의 미커밋 제품 2파일은 기술 담당 소유로 보존하며, 기획 담당은 이 명세 파일만 명시적으로
  커밋한다. 구현·통합 책임자는 기술 담당(어댑터 `src/lopec-sim.js`, 필요 시 `src/lopec-sandbox.js`). 1차 개정본은 배포 보류하고 2차까지 포함한 최종 형태로 운영 검증·출시한다.
  인수 조건은 9절 13~20항.
- 3차 개정(phase B 후보 축소, 2026-09-06): 같은 브랜치·워크트리, 같은 규칙(기획은 명세 파일만 커밋, 미커밋 제품 2파일 보존). 구현·통합 책임자는 기술 담당(어댑터
  `src/lopec-sim.js`, 필요 시 `src/lopec-sandbox.js`). 인수 조건은 9절 13~23항이며 18항의 phase B 판정은 21항이 대체한다.
- 4차 개정(재련 준비 데이터 재사용, 2026-09-06): 같은 브랜치·워크트리, 같은 규칙(기획은 명세 파일만 커밋, 미커밋 제품 2파일 보존). 구현·통합 책임자는 기술 담당(어댑터
  `src/lopec-sim.js`, 필요 시 `src/lopec-sandbox.js`). 진단용 문자열 치환 래퍼를 그대로 제품에 옮기지 않는다. 인수 조건은 9절 13~26항. **결과: 미채택** — 출시 후보는
  3차 최종 형태이며 워크트리의 4차 작업본은 3차 검토본(`src/lopec-sim.js` SHA256 912704BA…, `src/lopec-sandbox.js` 895141E8…)으로 되돌린 뒤 해시로 확인한다.

## 9. 검증 기준

코어 인수 조건(기술 담당, 병합 요청 전)
1. 딜러 3명(블레상돈 · 건슬상돈 · 초코상돈): `kind`·`option`·`finalScore`가 같은 시점의 로펙 표와 일치(점수 오차 ≤ 0.01), `baseScore`가 페이지 점수와 일치.
2. 모든 행에서 `per100k`가 확정된 정의와 일치(기대식 기준 오차 ≤ 0.01).
3. 서포터(꿈지향) · 격리 시간 초과(인위적 1ms 상한) · 시세 묶음 전부 실패 → `null`, 예외 전파 없음, 호출 프로세스 생존. (없는 캐릭터는 명령이 먼저 걸러 코어에 오지 않는다.)
4. 캐시가 빈 상태에서 캐릭터 1명 계산 시 **시세용 공식 API 호출 ≤ 14회**(+ 명령의 존재 확인 1회), 5분 안 재호출 0회(호출 카운터로 확인). 우선순위(보석 → 각인서 →
   재련 재료)대로 예산을 쓰고 넘친 키는 `sourceByKey`가 `lopec`.
5. 수치 경계: 진짜 무료 후보(costValue 0 + 요구 키 전부 가용) → `per100k: null`이고 정렬 맨 앞 · `cost="0"`이지만 요구 키가 빠진 후보 → `droppedForPrice` · 비용
   `NaN`/음수/무한대 후보 → 제외되고 집계 · 결과 JSON 문자열에 `Infinity`·`NaN` 부재 · 소수 비용이 원값 그대로 전달 · 스톤·카르마 행은 `priceSource: 'fixed'`이고
   `expectedCost`가 costValue(골드, 스톤의 "개수" 문자열이 아님).
6. 빈 결과 구분: 후보 0개 → `rows: []`(비-null) · 후보 3개 중 비용 미확인 3개 → `null`과 로그 · 3개 중 1개 → 행 2개 + `droppedForPrice: 1`.
6-1. 2단계 고정: 같은 시각의 로펙 표와 ① 후보 식별자 집합이 같고, ②에서 시세를 바꿔도(합성 시세 3벌) 표시 후보 집합과 `finalScore`가 변하지 않으며 `expectedCost`만
   바뀐다. 식별자 미발견은 `droppedForPrice`로 집계.
6-2. 시각·stale: 로펙 `isStale` 응답을 쓰면 `priceStale: true` · 시장 키를 하나도 안 쓴 결과(전부 fixed)는 `pricedAt: null` · `multi:` snapshotId는 최소 시각으로 파싱.
7. 시세 공유·재시도: 서로 다른 캐릭터 2명을 동시에 요청하면 겹치는 조회 묶음은 API 호출 1회 · 한 묶음을 인위적으로 실패시킨 뒤 다음 요청에서 그 묶음이
   재시도되고(실패 미캐시) 성공하면 5분 캐시에 들어감.

통합 검증(통합 책임자)
8. 디스코드: 임베드 설명 ≤ 4,096자, 필드 ≤ 1,024자, 상위 10개·반올림·`*` 표시(`fixed` 행은 `*` 없음)·0G 행 `무료 · —`·푸터 7항목의 조건부 표시(`pricedAt` null이면 시각
   생략, `priceStale`·`fixed`·`droppedForPrice` 문구)가 2절과 일치.
9. 카톡: 후보 12개 → 미리보기 5개 + 전체12개·변경 후 총점. 후보 1~5개도 총점 전체 보기 링크 제공. 악세 등급 변경/유지/추가/제거와 알 수 없는 형식 원문·권장 목표 보존, 무료 단위·시세 표시·투자 효율 설명 한 번을 확인한다.
   실제 저장 표본에서 후속 안내 포함 ≤1,500자와 상위5개 보존, 긴 본문에서는 기존 한도·전문 보존을 검증한다. `/스펙업`·`.스펙업`·`/ㅅㅍㅇ`·`.ㅅㅍㅇ` 네 입력 모두 동작, 접두사 없는 `ㅅㅍㅇ`은 침묵.
10. 실패·빈 결과 문구 3종(캐릭터 없음 → 코어 미호출 · 코어 `null` · `rows: []`), 카톡 예산 초과 경로.
11. 다른 커맨드 무영향: 팔찌·젬효율 결과와 소요 시간이 변경 전과 동일(진입점 탐색 확장) · `kakaoFull` 없는 기존 명령(`/도움말`·`/랭킹`)의 카톡 본문과 전체 보기가
    변경 전과 바이트 단위로 동일(브리지 훅 회귀).
12. 재등록 dry-run: 정의 88개, 이름 중복 없음.

응답 시간 단축(2026-09-06 개정) 인수 조건 — 기술 담당. 기존 코어 45건·기획 16건은 **수정 없이** 통과해야 한다(공개 계약 불변의 증거).
13. 동등성: 블레상돈 50행 · 건슬상돈 43행 · 초코상돈 46행에 대해 입력 데이터·원본 모듈·시세·시각을 고정하고 `b317910`의 `getSpecupGuide`와 새 구현의 반환을
    깊은 비교 — `baseScore`·`pricedAt`·`priceStale`·`droppedForPrice`와 rows의 순서·`kind`·`option`·`finalScore`·`expectedCost`·`per100k`·`priceSource`가 모두 같다.
14. 연속 통신: 실제 격리 자식 하나(같은 PID)가 probe → phase A → phase B 세 메시지를 처리하고 입력이 닫히면 스스로 종료한다. 팔찌·아크그리드는 기존 단발
    경로로 동작하며 결과(15.62 / 9.41)와 소요 시간이 변경 전과 같다.
15. 시간 초과·수명·슬롯: 한 단계를 busy loop로 막으면 그 단계 시작 20초에 종료되고 600ms 뒤 제한 사용자 계산 자식이 없다(기존 preflight 방식) · 호스트 시세
    대기를 인위적으로 늘려 60초 수명을 넘기면 자식이 종료된다 · 두 경우 모두 슬롯이 돌아오고 다음 계산이 성공한다 · 출력 1MB 초과와 잘못된 메시지에도 같다.
16. 동시성: 스펙업 2건을 동시에 요청하면 둘째는 전용 슬롯 대기 뒤 실행되고 결과는 단독 실행과 같다 · 스펙업이 도는 동안 `/팔찌` 요청은 스펙업 대기열에 차단되지
    않으며, CPU 경쟁을 포함한 실제 응답 시간을 기록한다.
17. 운영 실측(출시 전 필수, 제한 사용자 래퍼): 예열 상태에서 새 캐릭터 2명의 전체 시간·단계별 시간·자식 최대 RSS를 기록하고 15초 목표와 비교한다(미달이면 수치와
    함께 보고, 자동 불합격은 아님). 디스코드·카톡 출력은 같은 입력에서 변경 전과 바이트 단위로 같고, 명령 정의 88개·재등록 불필요.

후보 재사용(2026-09-06 2차 개정) 인수 조건 — 기술 담당. 13~17항은 최종 형태에서 다시 통과해야 한다.
18. 동등성: 블레상돈 50행 · 건슬상돈 43행 · 초코상돈 46행에 대해 입력·원본 모듈·시세·시각을 고정하고, 세 단계(probe·phase A·phase B)의 반환 전체와 최종 공개 반환을
    1차 개정본(후보 재사용 전)과 깊은 비교 — 모두 같다. 디스코드·카톡 명령 payload도 바이트 단위로 같다. (3차 개정 이후 phase B의 판정은 21항을 따른다.)
19. 변이 없음·범위: 각 단계 평가 전후로 재사용 후보 데이터의 직렬화가 같고, 시세를 바꾼 phase B 뒤에도 같다. 재사용은 한 요청 안으로 한정된다 — 서로 다른 캐릭터를
    연속 요청하면 각각 새로 생성하고, 같은 캐릭터라도 새 요청(결과 캐시 만료 뒤)은 새로 생성한다. baseline 전체·비용 평가 함수·view 결과가 단계 간에 공유되지 않음을
    코드 검토로 확인한다.
20. 회귀·실패·운영: 기존 83건(코어 45·기획 16·세션 22) 수정 없이 통과 · 후보 생성 실패와 어댑터 경계 탐지 실패는 `null`과 로그로 끝나고 자식 종료·슬롯 반환·다음 계산
    성공 · 운영 제한 사용자 환경에서 고정 입력 A/B를 1차 개정본 대비 **각 3회 이상 반복**, 예열 상태 새 캐릭터 2명 이상의 실조회 시간·단계별 시간·최대 RSS, 단계별 20초·
    수명 60초 시간 초과 재측정. 15초 목표와 카톡 25초 이내(재요청 없음) 여부를 함께 기록하며 로컬 비율을 운영 단축률로 환산하지 않는다.

phase B 후보 축소(2026-09-06 3차 개정) 인수 조건 — 기술 담당. 13~17·19·20항은 최종 형태에서 다시 통과해야 한다.
21. 동등성(3차 규칙): 3명 고정 입력에서 probe·phase A 반환 전체는 R2와 깊은 비교로 같다. phase B는 `baseScore`, 후보 ID 집합, ID별 전체 필드(`costValue`·`requiredKeys`·
    `hasCostModel`·`fixedOnly`·`engravingGrade`·`cells` 등 반환하는 모든 필드)가 같고 내부 배열 순서는 비교하지 않는다. 최종 공개 반환·최종 추천 순서·디스코드·카톡 명령
    payload는 바이트 단위로 같다. 누락 시세(예: 58개 키 누락) 시나리오·빈 ID·없는 ID에서 `droppedForPrice`와 `null`/`rows: []` 규칙이 R2와 같다.
22. 번들 검증·탐지: 새 모듈 묶음의 첫 요청에서 축소 B와 전체 B를 모두 계산해 선택 ID별 값이 같으면 검증됨으로 표시하고 다음 요청부터 축소 B만 계산한다(자식 호출 수·
    단계 시간으로 확인). 원본 평가 본문을 후보 간 누적이 생기도록 바꾼 fixture에서는 불일치를 감지해 로그를 남기고 그 번들에서 전체 B로 동작하며 결과는 전체 B와 같다.
    프로세스 재시작·배포 키 변경 시 검증이 다시 실행된다. 기존 기본값·배선 변경 3종은 여전히 자식 생성 전에 거절된다. 원본 배열은 제자리 변경되지 않는다.
23. 회귀·운영: 기존 83건 + 2차 13건 수정 없이 통과 · 운영 제한 사용자 환경에서 고정 입력 A/B(R2 대비) 각 3회 이상, 예열 상태 새 캐릭터 2명 이상 실조회·단계별
    시간·최대 RSS, 20초/60초 시간 초과·1MiB·프로토콜 오류 복구, 동시 2건과 팔찌 비차단 재측정. 15초 목표 달성 여부와 카톡 25초 이내(재요청 없음) 여부 기록. 공식 API
    포함 실조회를 배포 전에 할 수 없으면 배포 직후 실제 브리지로 측정하고 복구 기준(두 조회 모두 25초 초과 또는 오류 → 이전 소스 복구)을 사전에 정한다.

재련 준비 데이터 재사용(2026-09-06 4차 개정, **미채택 — 기록용**) 인수 조건 — 기술 담당. 4차를 채택하지 않았으므로 출시 인수 조건은 13~23항이다.
24. 동등성·경계: 3명 고정 입력에서 세 단계 반환 전체와 최종 공개 반환이 R3와 깊은 비교로 같고(B는 21항 규칙), 양 플랫폼 명령 payload가 바이트 단위로 같다. 시세 교체
    (항목별 배율 교차)·전부 0·일부 누락에서도 같다. 캐시 키 경계: 지원하지 않는 입력은 우회해 원본과 같은 결과, 캐릭터 ID·후보 ID만으로 키를 만들지 않음을 코드 검토로 확인.
    캐시 히트를 원본 재실행과 대조하는 감사 모드로 3명 전 구간 불일치 0, 저장 직후 스냅샷과 단계 종료 시점 비교로 캐시 객체·원본 인자 비변이. **상급 재련 후보가 실제로
    생성되는 표본 1명**을 찾아 동등성에 포함하고, 찾지 못하면 그 사실과 탐색 범위를 기록한다.
25. 활성화·복귀: 처음 보는 번들의 첫 성공 요청에서 캐시 끔/켬 phase A 대조 후 일치 시에만 활성화(자식 호출 수·단계 시간으로 확인), 준비 함수 본문을 바꿔 결과가 달라지는
    fixture에서는 불일치를 감지해 최적화를 끄고 3차 계산과 같은 결과를 내며 로그를 남긴다. 시그니처·배선 변경 3종은 활성화 전에 거절된다. 재시작·번들 변경 시 재검증하고
    parser가 바뀌면 캐시가 비워진다(새 요청·다른 캐릭터·5분 만료 후). 첫 요청(B 검증+A 검증)의 시간을 별도 기록하고 카톡 25초를 넘기면 두 요청으로 나눈 설계로 바꾼다.
26. 회귀·운영: 기존 106건 수정 없이 통과 · 운영 제한 사용자 환경에서 R3 대비 고정 입력 각 3회 이상, 첫 번들 검증 경로 시간, 예열 새 캐릭터 2명 이상 실조회·단계별
    시간·최대 RSS(256MB 상한 대비), 20초/60초·1MiB·프로토콜 오류 복구, 동시 2건·팔찌 비차단 재측정, 팔찌 15.62·젬 9.41. 공식 API 포함 실조회와 카톡 25초 판정은 23항의
    배포 직후 검증 규칙을 따른다.

페온 포함(2026-09-06 5차 개정) 인수 조건 — 코어 27·28은 기술 담당, 29는 기획 담당.
27. 옵션 연결·회귀: `crystalPrice95`를 넘기면 세 단계 옵션이 `excludePeonCost:false, peonCrystalPrice95Gold:P`이고 반환에 `crystalPrice95: P`가 실린다. 넘기지 않거나
    0·음수·비수면 기존과 완전히 같다(기존 코어·세션·후보 축소 테스트 전부 본문 무수정 통과, `crystalPrice95: null`). 결과 캐시는 (닉, P)별 — 같은 닉 다른 P는 재계산,
    같은 P는 5분 캐시. 번들 검증(3차)은 P와 무관하게 동작한다.
28. 로펙 동등성(페온 포함): 고정 입력 3명에서 P=17,501일 때 악세 행 `expectedCost`가 페온 제외 대비 정확히 35 × P ÷ 95 × 8.5 증가하고, 스톤 행은 기대 개수 × 9 × P ÷ 95 × 8.5
    증가하며, 보석·강화·카르마 행과 모든 `finalScore`는 불변. 후보 집합·점수는 로펙 UI에 같은 P를 입력한 표와 같다(실 페이지 1회 대조 — 2026-09-06 블레상돈 P=17,501:
    페온 제외 1순위 반지2 1.28만 → 포함 시 1순위 목걸이 8.36만·2순위 반지2 9.66만·3순위 반지2 6.76만, 강화 62.51만·보석 35.96만 불변).
29. 명령·표시: `/크리스탈` 조회(미설정·설정·7일 초과 3종 문구), 설정(범위 밖 거절, 저장 파일 원자 저장·설정자·시각), 카톡 `/크리스탈 16626`·`ㅋㄹㅅㅌ` 동작. 스펙업 푸터
    3종(포함·오래됨·제외)이 2절 순서대로 나오고 카톡 전체 보기에도 같은 줄이 있다. 정의 91→93, 재등록 필요. 시세를 바꾼 직후 `/스펙업`이 새 값으로 계산됨을 확인.
30. 자동 갱신 코어(기술, 6차): 고정 자료로 `fetchCrystalPrice95()` → `{ price95: 16910, candleAt: 2026-09-07 00:00 KST }`; 빈 배열·`close` 비수·범위 밖(999·500,001)·오래된 캔들(4일 전)·HTTP 오류·5초 초과 → `null`(예외 없음).
    스케줄러: 시작 즉시 1회 실행 → `setAuto` 호출, 다음 실행이 08:00 KST 정각, 실패 시 1시간 뒤 재시도·성공 후 정규 일정 복귀, 수동값 24시간 안이면 `saved:false`로 건너뜀(값 불변), `stop()` 뒤 타이머 없음.
31. 저장·표시(기획, 6차): `setAuto`가 24시간 안 수동값을 덮지 않고 24시간 뒤·자동값 위에는 덮음, 옛 파일(source 없음) = manual. `/크리스탈` 조회 4종(미설정·자동·직접·7일 초과)과 저장 문구, 푸터 4종
    (자동 포함·직접 포함·오래됨·미수신)이 2절 문구 그대로이고 항목 안에 ` · `가 없다. 자리표시 상태에서 `src/index.js`가 기동되고 정의 95개가 dev `04c94d4`와 동일(재등록 불필요).
32. 통합(기획, 코어 교체 후): 실제 로아툴 1회 조회로 저장 → `/크리스탈`이 `자동 갱신 · 로아툴`로 표시, `/스펙업` 푸터에 `(로아툴 M/D 자동)`, 직접 설정 뒤 자동 갱신이 24시간 안에 덮지 않음. 기존 페온·카톡 훅 테스트 무수정 통과.

## 10. 배포 전제·위험·결정 기록

배포 전제: `npm run register`(86 → 88개, `b317910`으로 완료). 환경변수·서버 설정·폰 스크립트 변경 없음. 응답 시간 단축 개정(2026-09-06)은 재등록·환경변수·서버
설정·폰 스크립트 변경이 없다.

위험
- 로펙 배포마다 모듈 ID·청크 경로가 바뀐다 → 팔찌 때와 같이 소스 패턴 역추적, 실패하면 `null`과 로그. 스펙업 청크가 동적 로딩이라 탐색 범위가 넓다.
- 시세가 로펙과 달라 `점수/10만G` **순위**가 로펙 표와 다를 수 있다 → 의도된 차이, 푸터에 출처 명시.
- 공식 API 분당 100회를 운영 봇 전체가 공유 → 전역 5분 캐시와 캐릭터당 ≤ 15회 상한.
- 로펙 페이지 요청은 캐릭터당 1회(5분 캐시)로 기존 `/로펙`·`/팔찌`와 같은 수준이다. 청크는 배포 키가 바뀔 때만 다시 받는다.
- 자식 재사용: 단계 사이 호스트가 시세를 기다리는 동안 자식이 유휴 상태로 슬롯을 점유한다(최대 60초 수명). 프로토콜 오류(잘못된 메시지·부분 출력)는 시간 초과와
  같은 종료·슬롯 반환 경로로 처리한다. 예열 상태에서도 20초 안팎이 남을 수 있으므로 카톡의 재요청·보류 경로는 계속 유지한다.
- 페온 포함: 크리스탈 시세는 로아툴 자동 갱신(6차) 또는 사람이 적는 값이다 → 7일 넘게 갱신이 없으면 푸터와 `/크리스탈` 조회에 안내, 범위 밖 값은 저장 거절(자동값도 같은 범위 검사). 잘못된 값을 적어도
  영향은 악세·스톤 후보의 비용·순위뿐이며 출처(설정자 닉 또는 로아툴)·시각이 남고, 직접 적은 값은 24시간 뒤 자동 갱신이 덮는다. 로아툴이 멈추면 마지막 값을 유지한 채 안내만 붙는다.
- 후보 재사용: 원본 후보 생성기 입력 자리에 미리 만든 데이터를 넣으므로 로펙 배포 시 어댑터 결합이 한 곳 늘어난다 → 경계 탐지 실패는 `null`과 로그. 이후 평가가
  후보 객체를 변경하면 단계 간 결과가 달라질 수 있어 인수 조건 19항으로 막는다.
- phase B 후보 축소: 원본이 후보 간 비용 누적을 도입하면 축소 B가 조용히 어긋날 수 있다 → 번들 검증 단계(3절)로 첫 요청에서 전체 B와 대조하고 불일치 시 전체 B로
  동작·로그. 동점 행의 B 내부 순서 차이는 비계약이며 최종 순서에 전달되지 않는다(정규화가 ID 색인과 A 순서를 사용).
- 재련 준비 데이터 재사용: 원본 풀이 내부 함수에 결합하므로 로펙 배포로 준비 함수의 export·배선·본문이 바뀌면 재사용이 어긋날 수 있다 → 시그니처 검사 + 첫 요청 끔/켬
  대조로 활성화하고, 알 수 없는 변경은 최적화만 꺼서 3차 속도로 돌아간다(기능은 유지). 캐시가 늘리는 메모리는 관측 최대 약 124MiB로 256MB 상한 안이지만 모든 입력의
  상한은 아니므로 26항에서 다시 잰다. 상급 재련은 범위 밖이라 그 후보가 많은 캐릭터는 4차 이득이 작을 수 있다.

결정 기록
- 2026-09-06 A안(점수 증가량 기준) · 1안(로펙 모듈 재사용, 로펙과 같은 숫자) · 비용은 공식 API 실시간 시세 · 옵션 없이 로펙 기본값 고정 · 상위 10개 ·
  최종 점수와 증가분 병기 · 실패는 `null` 통일 · 시세는 호스트 주입 · 격리 동시 2개 상한 · 스펙 위치 `specs/`, 계획은 `specs/plans/`.
- 2026-09-06 Phase 0 결과 반영(기술 담당 7건): 시세는 `tX` 옵션의 두 스냅샷으로 주입하고 `tJ` 둘째 인자(보유 수량)는 0 · `expectedCost`는 costValue, `priceSource`에
  `fixed`(스톤·카르마 고정 모델) 추가 · 공식 API는 시세 14회 + 존재 확인 1회, 보충 사유에 매핑 불명·예산 한도 포함, 보충 경로 2개 · `pricedAt`은 시장 시세 원본 시각의
  최소이며 전부 fixed면 null, `priceStale` 추가 · 후보는 로펙 시세로 식별자를 고정하고 우리 시세로 costValue만 재평가하는 2단계 · `cost="0"`은 무료가 아니며 요구 키
  가용성으로 판정 · per100k 식 유지 · 악세 target은 v1에서 로펙 시세 유지.
- 2026-09-06 스펙 검토 반영(기술 담당 5건 + 문구 2건): 소수 비용 허용과 0G 행 규칙(`per100k: null`, 맨 앞, `무료 · —`) · 정상 빈 결과와 시세 누락 실패의 구분
  (`droppedForPrice`) · 카톡 미리보기 10개와 전체 보기 전체 행 분리(`kakaoFull` 훅) · 시세 조회 묶음 단위 진행 중 요청 공유와 실패 미캐시 · Go 조건 ④ 후보·점수 일치 ·
  A안과 정렬 지표의 관계 명시 · 캐릭터 없음은 명령이 공식 API로 먼저 판별.
- 2026-09-06 응답 시간 단축(예열 후 운영 측정 근거, 기술 담당 검토 반영): (가) phase B를 없애고 호스트에서 비용을 재계산하는 안은 기각 — phase A에 재료 수량·고정
  골드 분해가 없고 원본 재련 전략이 시세를 입력으로 써서 Σ수량×새 단가와 동등하지 않다. 각인서·보석·악세만 분리하는 안은 이번 범위에서 보류. 채택: (나) 격리 자식
  1회 기동 + (라') 스펙업 전용 동시 1(전역 2 유지). 결과 캐시 5분 유지. 15초는 목표이며 절감 폭 미측정. 구현·통합 책임자는 기술 담당(`src/lopec-sim.js`·
  `src/lopec-sandbox.js`), 명세·인수 조건은 기획 담당, 브랜치 `feat/specup-latency`.
- 2026-09-06 2차 개정(후보 재사용): 1차 개정의 기획 교차 검토는 통과(P1/P2 없음)했으나 운영 약 14% 단축으로 15초 목표 미달. 기술 담당 진단으로 시세 독립적인 원본
  후보 생성이 요청당 6회 반복됨을 확인(로컬 31~34% 추가 단축, 세 단계·최종 deepEqual). 결정: 1차 개정본 배포 보류, 같은 브랜치에 후보 재사용을 추가해 최종 형태로 한 번에
  운영 검증·출시. 재사용 범위는 한 요청 안의 원본 후보 데이터만, baseline 전체·비용 평가 함수·view 결과 공유 금지, 요청 간 영구 저장 미포함. 구현·통합은 기술 담당, 명세·
  인수 조건 18~20은 기획 담당. 기획은 명세 파일만 커밋하고 미커밋 제품 2파일은 보존.
- 2026-09-06 3차 개정(phase B 후보 축소): 3차 진단에서 점수 메모·시세 선조회는 기각(실익 0.08초 / 후보 의존 POST 병목·예산 양립 불가), 병목은 재련 전략 풀이로 확정.
  phase B 후보 축소 진단은 운영 고정 입력 13.1→8.3 / 12.0→7.7 / 14.1→8.7초, ID별 값·최종 출력 일치, B 내부 동점 순서만 차이. 결정: phase B 내부 배열 순서는 비계약,
  ID별 전체 값·최종 반환·출력은 엄격 동일 유지, 번들 검증 단계로 원본 의미 변경을 탐지해 불일치 시 전체 B로 동작. 재련 전략 내부 분해와 카톡 전용 예산 상향은 3차 실측
  뒤 필요할 때만. 구현·통합은 기술 담당, 명세·인수 조건 21~23은 기획 담당. 1·2·3차를 최종 형태로 한 번에 운영 검증·출시.
- 2026-09-06 4차 개정(일반 재련 준비 데이터 재사용): 기술 담당 진단(운영 고정 입력 R3 대비 약 24% 추가 단축, 동등성·비변이·시세 경계 확인)과 사용자 방향(출시 전 성능
  우선)에 따라 채택. 범위는 한 요청 안의 확률 프로필·보조재 옵션 결과만, 시세가 붙은 값·정책·baseline·view 재사용 금지, 상급 재련 범위 밖. 활성화는 시그니처 검사 + 처음
  보는 번들의 첫 요청 끔/켬 대조, 실패 시 최적화만 끄고 3차 계산. 이후 남는 시간은 원본 풀이 자체라 4차를 출시 전 마지막 코어 최적화로 정한다. 구현·통합은 기술 담당,
  명세·인수 조건 24~26은 기획 담당. 1~4차를 최종 형태로 한 번에 운영 검증·출시.
- 2026-09-06 4차 개정 미채택: 정식 구현·검증(로컬 132건, 4명 동등성, 독립 검토 P1/P2 없음)은 끝났으나 운영 고정 입력에서 R3 대비 일관된 단축이 없고(−4.6% / +0.3% /
  +0.6%) 검증 요청 21.5초·RSS 128MiB(첫 검증 포함 135.26MiB)·복잡도가 늘어 기술 담당이 보류를 권고, 기획이 동의. 결정: 4차 제품 반영 없음, 출시 후보 = 3차 최종 형태(R1+R2+R3, 기획 교차 검토
  13~23 통과본). 4차 작업본은 기록으로만 보존(임시 폴더). 성능 추가 개선은 출시 뒤 실측을 본 다음 별도 판단.
- 2026-09-06 5차 개정(페온 포함): 사용자 지적 "어빌리티 스톤은 페온 값을 더해야 하지 않나"가 타당 — 페온 제외 모델은 스톤(50G/개)·악세 교체 비용을 실제보다 크게
  낮게 잡는다(예: 97돌 기대 735개 × 9페온). 자동 시세 조사 결과: 공식 오픈 API에 크리스탈 환율 없음 · 로아도구(LOSPI)는 `/api/crystal-history`를 쓰지만 응답이 암호화돼
  있고 직접 호출은 500 · 로아와·로아차트는 봇 요청 차단 · 로펙은 "크리스탈 가격"을 사용자가 직접 입력(95개 기준, 입력 시 악세·스톤에만 페온 골드 가산). 결정: 자동
  소스 대신 `/크리스탈`로 길드가 적어 두는 95개 가격을 쓰고, 설정이 있으면 페온 포함(로펙 페온 포함 보기와 동일), 없으면 기존 페온 제외. 분담: 명령·저장·표시·문서 =
  기획, 코어 옵션 연결·캐시 키·반환 필드·동등성 테스트 = 기술. 브랜치 `feat/specup-peon`(dev 기준).
- 2026-09-07 6차 개정(크리스탈 시세 자동 갱신): 사용자 제안 "하루마다 긁어서 저장해 두고 쓰면 안 되나" 수용. 5차 조사의 "자동 소스 없음"은 짧은 간격의 반복 탐색으로 차단(로아와 403·로아차트 429)된
  결과였고, 브라우저 UA로 1회씩 다시 확인하니 로아툴(LOSPI) `GET https://loatool.taeu.kr/api/crystal-history/ohlc/1d`가 인증·복호화 없이 일봉 63일치(5.6KB, `[{ dt, open, high, low, close }]`, dt는 KST 벽시계)를
  준다(`/1h` 시간봉·`/1` 분봉도 있음. 암호화된 `/api/crystal-history`는 쿠키 `__gpl`의 클라이언트 생성 키로 푸는 별도 경로라 쓰지 않는다). 검증: 9/5 종가 17,500G = 사용자가 9/6 로펙에 넣은 17,501G → 단위는
  95개당 골드. robots.txt 전체 허용, 별도 약관 페이지 없음(회색 지대는 떠상의 클로아보다 가벼움, 사용자 인지·승인). 결정: 시작 시 1회 + 매일 08:00 KST 1회 조회, 마지막 캔들 종가 = 현재가로 저장(source 'auto'),
  직접 설정은 24시간 우선, 실패 시 마지막 값 유지 + 1시간 뒤 재시도, 푸터·`/크리스탈`에 출처 표기. 분담: 저장 규칙(`setAuto`)·문구·푸터·진입점 배선·문서 = 기획, 조회·검증·스케줄러 `src/crystal-auto.js` = 기술
  (자리표시를 통째로 교체). 정의 변경 없음 → 재등록 불필요. 브랜치 `feat/crystal-auto`(dev `04c94d4` 기준).
